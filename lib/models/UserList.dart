class UserList {
  int statusCode;
  String token;
  String latitude;
  String longitude;
  double distanceInMeter = 0.00;

  UserList({this.token, this.latitude, this.longitude,this.distanceInMeter});
}
